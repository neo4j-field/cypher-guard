[package]
name = "cypher-guard"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
pyo3 = { version = "0.21", features = ["extension-module"] }
thiserror = "1.0"

[lib]
crate-type = ["cdylib", "rlib"]
